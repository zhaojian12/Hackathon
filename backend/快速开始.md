# Hackathon AI 助手 - 快速开始指南

## 📦 一键启动（推荐）

### Windows 用户

```bash
cd Hackathon\backend
start_server.bat
```

### macOS/Linux 用户

```bash
cd Hackathon/backend
chmod +x start_server.sh
./start_server.sh
```

脚本会自动：
- ✅ 检查 Python 环境
- ✅ 创建虚拟环境
- ✅ 安装依赖
- ✅ 检查 Ollama 服务
- ✅ 启动 AI 助手服务器

## 🔍 环境检查（推荐）

在开始之前，运行环境检查脚本：

```bash
cd Hackathon/backend
python check_setup.py
```

这会自动检查所有必需的依赖和配置。

## 🔧 首次使用配置

### 1. 安装 Ollama

**Windows:**
访问 https://ollama.com/download/windows 下载安装

**macOS:**
```bash
brew install ollama
```

**Linux:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 2. 启动 Ollama 服务

```bash
ollama serve
```

或者在 Windows 上双击 Ollama 图标启动。

### 3. 下载 AI 模型

```bash
# 推荐：Qwen2.5（中文友好，速度快）
ollama pull qwen2.5:7b

# 或者：DeepSeek Coder（代码相关问题更好）
ollama pull deepseek-coder:6.7b
```

### 4. 启动 AI 助手服务器

运行启动脚本（见上方）或手动启动：

```bash
cd Hackathon/backend
python ai_assistant_server.py
```

## ✅ 验证安装

### 1. 检查服务状态

访问：http://localhost:8000/health

应该看到：
```json
{
  "status": "ok",
  "engine": "Ollama",
  "model": "qwen2.5:7b",
  "ollama_status": "running"
}
```

### 2. 测试 AI 对话

```bash
curl -X POST http://localhost:8000/v1/assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"你好"}]}'
```

## 🎯 使用示例

### 常见问题

**Q: 如何连接钱包？**
```bash
curl -X POST http://localhost:8000/v1/assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"如何连接钱包？"}]}'
```

**Q: 如何获取测试币？**
```bash
curl -X POST http://localhost:8000/v1/assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"如何获取测试币？"}]}'
```

**Q: 交易需要多长时间？**
```bash
curl -X POST http://localhost:8000/v1/assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"交易需要多长时间？"}]}'
```

## 🔍 常见问题

### Ollama 服务未运行

**症状：**
```
❌ 错误: 无法连接到 Ollama
```

**解决：**
```bash
# 启动 Ollama
ollama serve
```

### 模型未找到

**症状：**
```
⚠️ 警告: 模型 qwen2.5:7b 未找到
```

**解决：**
```bash
# 拉取模型
ollama pull qwen2.5:7b

# 查看已安装的模型
ollama list
```

### 端口被占用

**症状：**
```
❌ Address already in use
```

**解决：**
修改 `.env` 文件中的端口：
```env
PORT=8001
```

### 推理速度慢

**解决方案：**
1. 使用更小的模型：`ollama pull mistral:7b`
2. 确保有足够内存（建议 8GB+）
3. 使用 GPU 加速（自动检测）

## 📊 系统要求

### 最低配置
- CPU: 4 核心
- RAM: 8GB
- 磁盘: 10GB 可用空间
- 网络: 稳定的互联网连接（首次下载模型）

### 推荐配置
- CPU: 8 核心
- RAM: 16GB
- GPU: NVIDIA/AMD（可选，显著提升速度）
- 磁盘: 20GB SSD

## 🚀 下一步

1. ✅ 启动前端项目（见 `../frontend/README.md`）
2. ✅ 在前端集成 AI 助手组件
3. ✅ 测试完整的用户流程

## 📚 更多文档

- [完整 README](./README.md) - 详细配置和 API 文档
- [前端集成指南](../frontend/AI_ASSISTANT_SETUP.md) - 如何在前端使用
- [Ollama 文档](https://github.com/ollama/ollama) - Ollama 官方文档

## 💡 提示

- 首次推理会较慢（加载模型），后续会快很多
- 模型会在内存中保留 5 分钟，频繁使用时速度更快
- 可以同时运行多个模型，但会占用更多内存

---

**开发完成 ✅ | 版本 v1.0.0 | 2026-02-06**
